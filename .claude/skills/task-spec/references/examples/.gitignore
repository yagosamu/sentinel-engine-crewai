# Generated by `npm install` when the cross-engine portability test
# (tests/test-portability-e2e.sh, Step 7) materializes the TypeScript consumer.
# Deps are exact-pinned in package.json, so both are regenerable — never commit.
node_modules/
package-lock.json
